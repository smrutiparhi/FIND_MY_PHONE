/**
 * Shared React component library (RiskBadge, CaseStatusBadge, RecoveryActionCard, etc.)
 * is built out in Part 23 - Final UI/UX. This barrel is reserved so apps/web can
 * depend on @recoverai/ui from day one without a breaking import change later.
 */
export const UI_PACKAGE_READY = false;
